{
  "name": "claude-code-docs",
  "private": true,
  "scripts": {
    "check:docs": "bash scripts/check-doc-consistency.sh",
    "lint": "markdownlint-cli2",
    "format": "markdownlint-cli2 --fix"
  },
  "devDependencies": {
    "markdownlint-cli2": "0.22.0"
  }
}
